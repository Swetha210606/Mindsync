const express = require("express"); const router = express.Router(); const Booking = require("../models/Booking"); const User = require("../models/User"); router.get("/therapists", async (req, res) => { try { const counsellors = await User.find({ role: { $in: ["therapist", "intern"] } }).select("-password"); res.json(counsellors); } catch (err) { res.status(500).json({ message: "Error" }); } }); router.post("/book", async (req, res) => { try { const booking = new Booking(req.body); await booking.save(); res.json({ message: "Session booked successfully!", booking }); } catch (err) { res.status(500).json({ message: "Booking failed" }); } }); router.post("/book/bulk", async (req, res) => { try { const { sessions, corporateId, corporateName, organizationType } = req.body; const bookings = sessions.map((s) => ({ ...s, corporateId, corporateName, organizationType, status: "pending" })); await Booking.insertMany(bookings); res.json({ message: "Sessions booked!" }); } catch (err) { res.status(500).json({ message: "Bulk booking failed" }); } }); router.get("/bookings/patient/:patientId", async (req, res) => { try { const bookings = await Booking.find({ patientId: req.params.patientId }).sort({ createdAt: -1 }); res.json(bookings); } catch (err) { res.status(500).json({ message: "Error" }); } }); router.get("/bookings/therapist/:therapistId", async (req, res) => { try { const bookings = await Booking.find({ therapistId: req.params.therapistId }).sort({ createdAt: -1 }); res.json(bookings); } catch (err) { res.status(500).json({ message: "Error" }); } }); router.get("/bookings/corporate/:corporateId", async (req, res) => { try { const bookings = await Booking.find({ corporateId: req.params.corporateId }).sort({ createdAt: -1 }); res.json(bookings); } catch (err) { res.status(500).json({ message: "Error" }); } }); module.exports = router;
